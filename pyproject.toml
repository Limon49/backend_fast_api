[project]
name = "aiquest_app"
version = "0.1.0"
description = "AIQuest - a small FastAPI learning project"
readme = "README.md"
authors = [
    { name = "limonedutune", email = "limon@edutune.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.115",
    "pymysql>=1.2.3",
    "sqlmodel>=0.0.22",
    "uvicorn>=0.30",
]

[dependency-groups]
dev = []
