"""
Helper scripts — run them with ``uv run python -m app.scripts.<name>``.

``init_db``  run migrations, then (re)create the reporting view
``seed``     insert demo products so there is something to buy
``db_views`` the ``order_details`` view on its own
"""
